projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_RAP_C_DELIVERY_H //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Resume;
  use action Activate;
  use action Discard;
  use action Prepare;

  use association _items { create; with draft; }
}

define behavior for ZC_RAP_C_DELIVERY_I //alias <alias_name>
{
  use update;
  use delete;

  use association _header { with draft; }
}